# 注册表设计文档模板

> 当需要规划新的注册表或调整现有注册表时，使用此模板。
> 复制以下章节到工作文档中，并将占位符替换为真实内容。

---

## 1. 注册表概览

- **注册表名称：** `<core:example_registry>`
- **业务目标：** `<这个注册表解决什么问题？>`
- **主要负责人：** `<系统 / 功能 / 团队>`
- **关联内容范围：** `<物品 / POI / 任务 / 家园模块 / 商人 / 音频 / ...>`

## 2. ResourceLocation 规则

- **条目命名空间：** `<game / core / dlc_x / ...>`
- **条目 ID 命名规范：** `<game:example_entry_name>`
- **所需标签命名规范（如有）：** `<game:tag/example_tag>`
- **跨注册表引用：** `<允许引用哪些其他注册表？>`

## 3. 加载时机与生命周期

- **何时创建注册表？** `<startup / loading screen / 按需创建>`
- **何时注册条目？** `<阶段或函数>`
- **是否允许运行时扩展？** `<是/否/如何扩展>`
- **是否跨场景持久存在？** `<是/否>`

## 4. 条目结构

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| `<id>` | `<ResourceLocation>` | `<是/否>` | `<无>` | `<含义>` |
| `<field_a>` | `<type>` | `<是/否>` | `<value>` | `<含义>` |
| `<field_b>` | `<type>` | `<是/否>` | `<value>` | `<含义>` |

### 4.1 嵌套结构（可选）

| 嵌套字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `<nested.field>` | `<type>` | `<是/否>` | `<含义>` |

## 5. 校验规则

- `<规则 1：例如 —— 被引用的 ResourceLocation 必须存在>`
- `<规则 2：例如 —— 数值范围超出时需要夹取或拒绝>`
- `<规则 3：例如 —— 不允许重复 ID>`

## 6. 运行时访问方式

- **查询 API：** `<RegistryManager.get_registry(...) / 辅助封装 / 服务>`
- **典型调用方：** `<UI / 玩法运行时 / 存档系统 / 生成流程>`
- **缓存策略：** `<无 / 本地缓存 / 预计算映射>`
- **失败处理：** `<记录日志并跳过 / assert / 回退条目>`

## 7. 编写流程

1. `<源数据在哪里编写？>`
2. `<如何评审？>`
3. `<如何校验？>`
4. `<运行时代码如何消费？>`

## 8. 存档 / 迁移说明

- **是否直接保存条目 ID？** `<是/否>`
- **删除条目后的兼容策略：** `<回退 / 转换 / 返还>`
- **是否需要版本字段？** `<是/否>`

## 9. 示例条目

```json
{
  "id": "<game:example_entry>",
  "field_a": "<value>",
  "field_b": 0
}
```

## 10. 待确认问题

- `<问题 1>`
- `<问题 2>`
- `<问题 3>`

## 11. 实施检查清单

- [ ] 已确认注册表类型名
- [ ] 已确认 ResourceLocation 命名规则
- [ ] 已敲定条目结构
- [ ] 已记录校验规则
- [ ] 已记录运行时加载时机
- [ ] 已记录存档/迁移行为
- [ ] 已评审示例条目
